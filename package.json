{
  "name": "wallet-whitelist-api",
  "version": "1.0.0",
  "description": "Checks if a wallet belongs to a whitelist and returns the merkle proof.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["nft","whitelist","wallet","merkle"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.1",
    "keccak256": "^1.0.6",
    "merkletreejs": "^0.2.31"
  }
}
